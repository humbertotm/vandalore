export.log_in_local = function(req, res) {
    /*
        Extract email and password from req.body and store it in respective var.

        if(!(email && password))
            return message: 'Must provide email and password.'

        Find a user in Users collection with this email

        if(!user)
            return user not found.

        if(password === user.password)
            create token
            send user and token in res.
    */
};