export.sign_up_local = function(req, res) {
	/*
		if(!(email && username && password && passwordConfirmation))
			return message: 'Must provide all of these shits.'

		Find user in Users collection with req.body.email

		if(user)
			return message: 'Email already exists.'

		Save user to database.

		On success:
			create jwt token
			send user and token in res.
			send activation email.

		On error
			return error message.
	*/
};